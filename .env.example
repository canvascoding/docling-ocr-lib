# Docling runs locally, no API key required.
# These env vars are only used if you want to pre-download models for offline use.
# DOCLING_ARTIFACTS_PATH=/local/path/to/models

# S3 storage (optional)
S3_BUCKET=
S3_REGION=eu-central-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=